name: Kotlin Maven Codecov
run-name: ${{ github.actor }} is testing out Kotlin with Codecov 🚀
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test-and-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Where am I?
        run: pwd
      - name: What is here?
        run: ls ${{ github.workspace }}
      - name: Checkout
        uses: actions/checkout@v2
      - name: What is here now?
        run: ls ${{ github.workspace }}
      - name: cd to kotlintest.
        run: cd /home/runner/work/codecov-poc/codecov-poc/kotlintest
      - name: What is in kotlintest?
        run: ls
      - name: Set up JDK 18
        uses: actions/setup-java@v1
        with:
          java-version: 18
      - name: Install dependencies
        run: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
      - name: Run tests and collect coverage
        run: mvn -B test
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
